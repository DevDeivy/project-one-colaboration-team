package com.example.project_one_colaboration_team.caps.repository;

import com.example.project_one_colaboration_team.caps.model.Cap;
import org.springframework.data.jpa.repository.JpaRepository;

public interface CapRepository extends JpaRepository<Cap, Long> {
}
